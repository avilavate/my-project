/**
 * @param {number[]} digits
 * @return {number[]}
 */
var findEvenNumbers = function (digits) {
  let evens = digits.filter(digit => digit % 2 === 0)
  let odds = digits.filter(digit => digit % 2 !== 0)
 
  let result = []
  
}
