/**
 * @param {number} n
 * @return {number}
 */
var bulbSwitch = function (n) {
  let nMax = n
  let bulbs = Array(n).fill(0)
  const game = n => {
    if (n === 0) return 0
    if (n === 1) return 1
    if (n === 2) return 1
   for (let i = 0; i < nMax; i++) {
   
   }
  }
}
